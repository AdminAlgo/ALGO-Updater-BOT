"""Admin web dashboard — companies, driver-group assignment, status.

Additive on top of the existing scheduler/registry objects; the Telegram
admin commands (/add /remove /drivers /coverage) keep working unchanged
alongside it. See src/dashboard/app.py for the Flask app factory.
"""
